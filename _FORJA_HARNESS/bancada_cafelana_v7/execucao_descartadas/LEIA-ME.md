# Execuções descartadas — preservadas como evidência

## `opus-alias-resolveu-4.8`

Primeira execução do participante `opus-5`, feita com o alias curto `--model opus`.
O envelope do Claude Code reportou `claude-opus-4-8`: o alias não resolve para o
Opus 5 nesta instalação.

A peça não foi descartada por ser ruim — 10.956 palavras, sem truncamento. Foi
descartada por não ser do modelo que a bancada se propôs a medir. Sem o gate de
identidade, o relatório teria atribuído ao Opus 5 o trabalho do Opus 4.8.

Endereço correto, confirmado por sonda: `claude-opus-5`. O `opusplan` resolve
para `claude-sonnet-4-6`, e `opus-5` não é reconhecido.

Lição da casa que isso confirma: identidade de modelo se lê no envelope, nunca
no que se pediu.
